import type { NextConfig } from "next";

// Sin PA API no hay fotos de producto, así que no hay ningún host remoto que
// permitir todavía. Cuando llegue la API se añade aquí y ProductoImagen se
// enciende solo, sin tocar ninguna página.
const nextConfig: NextConfig = {};

export default nextConfig;
